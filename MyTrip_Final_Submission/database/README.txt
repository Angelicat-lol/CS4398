MyTrip uses an embedded H2 database.

The database is created automatically when the application is first run.

Configuration:
spring.datasource.url=jdbc:h2:file:./data/mytrip

The generated data directory is intentionally excluded from the submission.
The H2 console is available at:
http://localhost:8080/h2-console
